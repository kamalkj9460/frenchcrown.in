{%- if collection.handle contains 'shirt' -%}
  {%- assign price_tag_lists = section.settings.shirt_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'suit' -%}
  {%- assign price_tag_lists = section.settings.suit_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'blazer' -%}
  {%- assign price_tag_lists = section.settings.blazer_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'accessories' -%}
  {%- assign price_tag_lists = section.settings.accessories_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'footwear' -%}
  {%- assign price_tag_lists = section.settings.footwear_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'belt' -%}
  {%- assign price_tag_lists = section.settings.belt_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'wallet' -%}
  {%- assign price_tag_lists = section.settings.wallet_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'waistcoat' -%}
  {%- assign price_tag_lists = section.settings.waistcoat_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'pant' -%}
  {%- assign price_tag_lists = section.settings.pant_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'shorts' -%}
  {%- assign price_tag_lists = section.settings.shorts_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'jeans' -%}
  {%- assign price_tag_lists = section.settings.jeans_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'boxer' -%}
  {%- assign price_tag_lists = section.settings.boxer_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'lounge_pant' -%}
  {%- assign price_tag_lists = section.settings.lounge_pant_price_taglist | split: ','  -%}
{%- elsif collection.handle contains 'kurtas' -%}
  {%- assign price_tag_lists = section.settings.shirt_price_taglist | split: ','  -%}
{% else %}
  {%- assign price_tag_lists = section.settings.shirt_price_taglist | split: ','  -%}
{%- endif -%}


{%- if price_tag_lists != empty -%}
  <div class="Collapsible Collapsible--padded Collapsible--autoExpand shirt-size">
    <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
      Price Range <span class="Collapsible__Plus"></span>
    </button>

    <div class="Collapsible__Inner">
      <div class="Collapsible__Content">
        <ul class="Linklist">
          {%- for tag_name in price_tag_lists -%}
            {%- assign tag_value = tag_name | handleize -%}
            <li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
              <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                {{- tag_name -}}
              </button>
            </li>
          {%- endfor -%}
        </ul>
      </div>
    </div>
  </div>
{%- endif -%}