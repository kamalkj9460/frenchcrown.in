
    	  {%- if collection.handle contains 'shirts' or collection.handle contains 'suits' or collection.handle contains 'blazers' or collection.handle contains 'kurtas' and collection.handle != 'tshirts' and section.settings.shirt_pattern_taglist != empty -%}
                <div class="Collapsible Collapsible--padded Collapsible--autoExpand">
                  <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                    {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
                  </button>

                  <div class="Collapsible__Inner">
                    <div class="Collapsible__Content">
                      <ul class="Linklist">
                      {%- assign  pattern_tag_lists = section.settings.shirt_pattern_taglist | split: ','  -%}
                      {%- for tag_name in pattern_tag_lists -%}

                        {%- assign tag_value = tag_name | handleize -%}
                        {%- assign flag = false -%}
                        {%- for tag in current_tags -%}
                          {%- if tag == tag_value -%}
                            {%- assign flag = true -%}
                          {%- endif -%}
                        {%- endfor -%}


                         <li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                              <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                                {{- tag_name -}}
                              </button>
                          </li>

                      {%- endfor -%}
                      </ul>
                    </div>
                  </div>
                </div>
    	
          {%- elsif collection.handle contains 'boxers' and section.settings.boxer_pattern_taglist != empty-%}
            <div class="Collapsible Collapsible--padded Collapsible--autoExpand">
              <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
              </button>

              <div class="Collapsible__Inner">
                <div class="Collapsible__Content">
                  <ul class="Linklist">
                    {%- assign  pattern_tag_lists = section.settings.boxer_pattern_taglist | split: ','  -%}
                    {%- for tag_name in pattern_tag_lists -%}

                    {%- assign tag_value = tag_name | handleize -%}
                    {%- assign flag = false -%}
                    {%- for tag in current_tags -%}
                    {%- if tag == tag_value -%}
                    {%- assign flag = true -%}
                    {%- endif -%}
                    {%- endfor -%}


                    <li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                      <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                        {{- tag_name -}}
                      </button>
                    </li>

                    {%- endfor -%}
                  </ul>
                </div>
              </div>
            </div>
    
    	  {%- elsif collection.handle contains 'lounge-pants' and section.settings.lounge_pants_pattern_taglist != empty-%}
            <div class="Collapsible Collapsible--padded Collapsible--autoExpand">
              <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
              </button>

              <div class="Collapsible__Inner">
                <div class="Collapsible__Content">
                  <ul class="Linklist">
                    {%- assign  pattern_tag_lists = section.settings.lounge_pants_pattern_taglist | split: ','  -%}
                    {%- for tag_name in pattern_tag_lists -%}

                    {%- assign tag_value = tag_name | handleize -%}
                    {%- assign flag = false -%}
                    {%- for tag in current_tags -%}
                    {%- if tag == tag_value -%}
                    {%- assign flag = true -%}
                    {%- endif -%}
                    {%- endfor -%}


                    <li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                      <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                        {{- tag_name -}}
                      </button>
                    </li>

                    {%- endfor -%}
                  </ul>
                </div>
              </div>
            </div>

          {%- elsif collection.handle contains 'lounge-shorts' and section.settings.lounge_shorts_pattern_taglist != empty-%}
            <div class="Collapsible Collapsible--padded Collapsible--autoExpand">
              <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
              </button>

              <div class="Collapsible__Inner">
                <div class="Collapsible__Content">
                  <ul class="Linklist">
                    {%- assign  pattern_tag_lists = section.settings.lounge_shorts_pattern_taglist | split: ','  -%}
                    {%- for tag_name in pattern_tag_lists -%}

                    {%- assign tag_value = tag_name | handleize -%}
                    {%- assign flag = false -%}
                    {%- for tag in current_tags -%}
                    {%- if tag == tag_value -%}
                    {%- assign flag = true -%}
                    {%- endif -%}
                    {%- endfor -%}


                    <li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                      <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                        {{- tag_name -}}
                      </button>
                    </li>

                    {%- endfor -%}
                  </ul>
                </div>
              </div>
            </div>

    	  {%- elsif collection.handle contains 'pants' and section.settings.trousers_pattern_taglist != empty -%}
               <div class="Collapsible Collapsible--padded Collapsible--autoExpand">
                <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                  {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
                </button>

                <div class="Collapsible__Inner">
                  <div class="Collapsible__Content">
                    <ul class="Linklist">
                    {%- assign  pattern_tag_lists = section.settings.trousers_pattern_taglist | split: ','  -%}
                    {%- for tag_name in pattern_tag_lists -%}

                      {%- assign tag_value = tag_name | handleize -%}
                      {%- assign flag = false -%}
                      {%- for tag in current_tags -%}
                        {%- if tag == tag_value -%}
                          {%- assign flag = true -%}
                        {%- endif -%}
                      {%- endfor -%}

                      <li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                        <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                          {{- tag_name -}}
                        </button>
                      </li>
                      
                    {%- endfor -%}
                    </ul>
                  </div>
                </div>
              </div>
    
    	  {%- elsif collection.handle contains 'waistcoats' and section.settings.waistcoat_pattern_taglist != empty -%}
               <div class="Collapsible Collapsible--padded Collapsible--autoExpand">
                <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                  {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
                </button>

                <div class="Collapsible__Inner">
                  <div class="Collapsible__Content">
                    <ul class="Linklist">
                    {%- assign  pattern_tag_lists = section.settings.waistcoat_pattern_taglist | split: ','  -%}
                    {%- for tag_name in pattern_tag_lists -%}

                      {%- assign tag_value = tag_name | handleize -%}
                      {%- assign flag = false -%}
                      {%- for tag in current_tags -%}
                        {%- if tag == tag_value -%}
                          {%- assign flag = true -%}
                        {%- endif -%}
                      {%- endfor -%}
                      	<li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                            <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                              {{- tag_name -}}
                            </button>
                        </li>
                    {%- endfor -%}
                    </ul>
                  </div>
                </div>
              </div>    
    
    	  {%- elsif collection.handle contains 'shorts' and section.settings.shorts_pattern_taglist != empty -%}
               <div class="Collapsible Collapsible--padded Collapsible--autoExpand">
                <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                  {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
                </button>

                <div class="Collapsible__Inner">
                  <div class="Collapsible__Content">
                    <ul class="Linklist">
                    {%- assign  pattern_tag_lists = section.settings.shorts_pattern_taglist | split: ','  -%}
                    {%- for tag_name in pattern_tag_lists -%}

                      {%- assign tag_value = tag_name | handleize -%}
                      {%- assign flag = false -%}
                      {%- for tag in current_tags -%}
                        {%- if tag == tag_value -%}
                          {%- assign flag = true -%}
                        {%- endif -%}
                      {%- endfor -%}

                      	<li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                            <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                              {{- tag_name -}}
                            </button>
                        </li>

                        

                    {%- endfor -%}
                    </ul>
                  </div>
                </div>
              </div>
    		
    	  {%- elsif collection.handle contains 'sale' and section.settings.sale_pattern_taglist != empty -%}
    			<div class="Collapsible Collapsible--padded Collapsible--autoExpand">
                <button type="button" class="Collapsible__Button Heading u-h6" data-action="toggle-collapsible" aria-expanded="false">
                  {{ section.settings.pattern_title }} <span class="Collapsible__Plus"></span>
                </button>

                <div class="Collapsible__Inner">
                  <div class="Collapsible__Content">
                    <ul class="Linklist">
                    {%- assign  pattern_tag_lists = section.settings.sale_pattern_taglist | split: ','  -%}
                    {%- for tag_name in pattern_tag_lists -%}

                      {%- assign tag_value = tag_name | handleize -%}
                      {%- assign flag = false -%}
                      {%- for tag in current_tags -%}
                        {%- if tag == tag_value -%}
                          {%- assign flag = true -%}
                        {%- endif -%}
                      {%- endfor -%}

                      	<li class="Linklist__Item {% if current_tags contains tag_value %}is-selected{% endif %}">
                            <button type="button" class="Link Link--primary Text--subdued {% if current_tags contains tag_value %}is-active{% endif %}" data-tag="{{ tag_value }}" data-action="toggle-tag">
                              {{- tag_name -}}
                            </button>
                        </li>

                        

                    {%- endfor -%}
                    </ul>
                  </div>
                </div>
              </div>
          {%- endif -%}
